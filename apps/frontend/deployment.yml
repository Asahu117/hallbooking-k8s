apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: hallbooking
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: anurag117/nhsrc-frontend:latest
        ports:
        - containerPort: 80
